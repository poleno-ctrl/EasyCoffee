# Домашнее задание 3. Работа с VCS GIT.
#### Команда Ахо-Карасики

###### Групповая динамика и коммуникации в профессиональной практике программной инженерии
Согласно исходному выданному ДЗ, необходимо задокументировать созданный репозиторий. 

1. Был создан новый репозиторий `https://github.com/poleno-ctrl/Grudina.git` в сервисе GitHub.
  1.1 Были распределены роли работы в репозитории, были выданы доступы к работе с удаленным репозиторием, чтобы каждый участник мог создавать свою ветку и делать PullRequest.

2. Была создана папка `docs/`, которая будет содержать markdown файлы и документации. Вносить изменения может каждый участник в свою ветку, далее делать PullRequest на ветку main. В случае, если предлагаемые изменения валидны, будет осуществлено слияние (merge) с основной веткой. Именоваться ветки будут согласно характеру изменений, вносимых в проект, то есть по типу `refactor`, `bugfix`, `hotfix`, `release`, `documentation`, `feature`, `launch`. Стиль написания коммитов формальный, немногословный, человекочитаемый, отражаемый суть вносимых изменений. 

3. Была создана папка `content/`, которая содержит анкету результатов собеседования компании разработчика и уточнения некоторых ответов от них по проекту «Кафетерий льгот».

4. Настроена система `approve'ов` в репозитории. Прежде чем изменения попадут в основную ветку, все сокомандники должны аппрувнуть предлагаемые изменеия, вследствии чего данные изменения попадут в основной релиз. Ветка main является `protected` веткой. Были проведены настройки ветки, согласно которым запрещвется делать push-коммиты в main, изменения требуют обязательного создания PullRequest, и далее аппрувов. 

5. Создан тэг с финальной версией нашего документа анкеты. Коммит, котрый будет использоваться при проверке нашей анкеты - ` b1fa73425622b580901bdfb80c32d34d35d157a1 `.
  
6. Соблюдены условия работы в репозитории - требования обязательного согласования с командой при создании PullRequest'а и сбора Approve'ов. Создаваемые ветки носят названия, связанные с содержанием файлов, загруженных в какую-либо ветку, т.е. согласно характеру изменений, вносимых в проект(`refactor`, `bugfix`, `release`).

7. Представлен итоговый README.md.

